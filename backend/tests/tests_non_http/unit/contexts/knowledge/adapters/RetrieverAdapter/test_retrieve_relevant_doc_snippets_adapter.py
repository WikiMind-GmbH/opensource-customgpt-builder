# class TestRetrieveDocSnippets:
#     @staticmethod
#     def test_raises_error_if_user_has_no_access_to_cgpt():

# RetrieverAdapter simply calls an orchestration function,
# so the functionality is mostly tested by the unit test of this orchestration function

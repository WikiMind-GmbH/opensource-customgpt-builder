The pictures show how we generally implement DDD-light + Hexagonal architecture in our backend folder utilizting python modules.
They do not show project specifics, like explicit contexts and ports, instead abstracting to how the different groups (orchestration modules, adapter modules port modules) relate to each other.

The boxes mostly represent actual python modules, often representing a group of modules instead of one singular one. If so, it is marked at the side of the box.
